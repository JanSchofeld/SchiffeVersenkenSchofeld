<UserControl x:Class="SchiffeVersenken.ChooseOpponentView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SchiffeVersenken"
        mc:Ignorable="d"
        Height="450" Width="800">

    <UserControl.Resources>
        <Style TargetType="Button">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Margin" Value="0,0,0,10" />
        </Style>
    </UserControl.Resources>
    
    <StackPanel Width="300" HorizontalAlignment="Center" VerticalAlignment="Center">
        <Button Command="{Binding VsPlayerCommand}" Content="Zwei Spieler"/>
        <Button Command="{Binding VsComputerCommand}" Content="Ein Spieler"/>
        <Button Command="{Binding BackToMenuCommand}" Content="Zurück zum Menü"/>
    </StackPanel>
    
</UserControl>
