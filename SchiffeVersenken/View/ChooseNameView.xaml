<UserControl x:Class="SchiffeVersenken.ChooseNameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SchiffeVersenken"
             mc:Ignorable="d" 
             Height="200" Width="300">

    <StackPanel Margin="10">

        <Label Content="Namen eingeben:" FontSize="20"/>
        <TextBox x:Name="nameTextBox" Margin="5" FontSize="20" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>
        <Button Content="Bestätigen" Margin="5" FontSize="20" Command="{Binding SetNameCommand}"/>

    </StackPanel>
</UserControl>
